extends layouts/main 

block content
    span 메일로 보냅시다
    .mailform-container 
        form(action=`/photo${routes.sendPhoto}`, method="post")
            label(for="email") 이메일을 입력해주세요
            input(type="text", id="eamil", name="email" ,required=true)
            input(type="submit", value="메일 보내기!")