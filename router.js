// GLobal(전역적인 것들)
const HOME = "/";

//Pictrue
const PHOTO = "/photo";
const FRAME = "/frame";
const CHOOSEPHOTO = "/chosephoto";
const UPLOAD = "/upload";
const SENDMAIL = "/sendemail";

const routes = {
  home: HOME,
  photo: PHOTO,
  choosePhoto: CHOOSEPHOTO,
  upload: UPLOAD,
  frame: FRAME,
  sendMail: SENDMAIL,
};

export default routes;
