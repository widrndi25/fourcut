// GLobal(전역적인 것들)
const HOME = "/";

//Pictrue
const PHOTO = "/photo";
const FRAME = "/frame";
const CHOOSEPHOTO = "/chosephoto";

const routes = {
  home: HOME,
  photo: PHOTO,
  choosePhoto: CHOOSEPHOTO,
  frame: FRAME,
};

export default routes;
